# Remove DB_SYNC=true for production!
DB_SYNC=false # Only for dev
# Whether to apply migrations/schema changes when run.
DB_MIGRATIONS=true
# Enable Cors for dev
CORS_ENABLED=false

# DB Connection
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=postgres
